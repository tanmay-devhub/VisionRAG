# ── VisionRAG Phase 4 v1: Video Support (Frame-Only) ─────────────────────────
# File: backend/requirements.txt
# Changes: Added ffmpeg comment (no new pip packages — ffmpeg is system binary)
# Image pipeline: UNTOUCHED — zero lines removed or modified
# ─────────────────────────────────────────────────────────────────────────────

fastapi
uvicorn[standard]
python-dotenv
neo4j>=5.0.0
langchain
langchain-text-splitters
langchain-ollama
fastembed>=0.3.0
rapidfuzz
numpy
Pillow
google-genai
openai
pymupdf
pdfplumber
python-multipart
pydantic-settings
httpx
json-repair

# Video support: ffmpeg must be installed system-wide
# brew install ffmpeg (Mac) / apt install ffmpeg (Linux) / winget install ffmpeg (Windows)
